<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Catégorie -->
        <record id="module_category_library" model="ir.module.category">
            <field name="name">Bibliothèque</field>
            <field name="description">Gestion de la bibliothèque</field>
            <field name="sequence">10</field>
        </record>

        <!-- Groupe Utilisateur -->
        <record id="group_library_user" model="res.groups">
            <field name="name">Utilisateur</field>
            <field name="category_id" ref="module_category_library" />
            <field name="comment">Peut consulter les livres, auteurs et catégories.</field>
        </record>

        <!-- Groupe Manager (hérite d'Utilisateur) -->
        <record id="group_library_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_library" />
            <field name="comment">Peut tout gérer.</field>
            <field name="implied_ids" eval="[(4, ref('group_library_user'))]" />
            <field name="users" eval="[(4, ref('base.user_root'))]" />
        </record>

        <!-- Règle -->
        <record id="rule_library_book_own" model="ir.rule">
            <field name="name">Livre : ses propres livres uniquement</field>
            <field name="model_id" ref="model_library_book" />
            <field name="domain_force">[('create_uid', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_library_user'))]" />
            <field name="perm_read" eval="True" />
            <field name="perm_write" eval="True" />
            <field name="perm_create" eval="True" />
            <field name="perm_unlink" eval="False" />
        </record>

    </data>
</odoo>